<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Deliberately NOT requesting android.permission.CAMERA: this app only ever
         calls react-native-image-picker's launchCamera(), which delegates to the
         system camera app via an implicit Intent when this permission is absent
         from the manifest, and that app handles its own permission prompt. If this
         permission IS declared, the library instead expects the CALLING app to
         have obtained it at runtime first (via PermissionsAndroid, which nothing
         in this codebase does), and throws instead of prompting when it hasn't
         been. Re-add this only alongside actual PermissionsAndroid.request() handling. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
